import { RegularFlexBoxDirective } from './regular-flex-box.directive';

describe('RegularFlexBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new RegularFlexBoxDirective();
    expect(directive).toBeTruthy();
  });
});
