import { RegularAppearAnimationDirective } from './regular-appear-animation.directive';

describe('RegularAppearAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new RegularAppearAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
